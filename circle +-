a=20

def setup():
    size(500,500)
    background(0)
    frameRate(10)
def draw ():
    background(0)
    global a
    if keyPressed:
        if key == "w":
            a=a+4
        elif key == "s":
            a=a-4
        circle(250,250,a)
        
